import {Dimensions} from 'react-native';

const SCREEN_SIZE = Dimensions.get('window');

export const SCREEN_WIDTH = SCREEN_SIZE.width;
export const SCREEN_HEIGHT = SCREEN_SIZE.height;

export const SCREEN_PADDING = 35;
export const SCREEN_PADDING_2 = 20;
